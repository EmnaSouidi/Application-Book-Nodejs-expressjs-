<%- include('parts/top.ejs') %>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>


<div class="container">
    <table class="table table-dark table-striped">
        <thead>
          <tr>
            <th scope="col">Image</th>
            <th scope="col">Title</th>
            <th scope="col">Author</th>
            <th scope="col">Price</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody>
          <% for( let book of mybooks)  {%>
          <tr>
            <th><img width="50" height="50" src="/uploads/<%= book.image%>"></th>
            <td><%= book.title%></td>
            <td><%= book.author%></td>
            <td>§<%= book.price%></td>
            <td>
             <!-- <a class="btn btn-danger" href="/mybooks/delete/<%= book._id%>">Delete</a>
                  <button class="btn btn-primary">Update</button> -->
                  <!-- si je travail avec le forme -->
                  <form  action="/mybooks/delete" method="POST">
                    <input type="hidden" name="bookId" value="<%=book._id%>">
                    <button class="btn btn-danger" type="submit">Delete</button>
                  </form>
                  
                  <a href="/mybooks/update/<%= book._id%>" class="btn btn-primary">Update</a>
              
              </td>
          </tr>
          <% } %>
        </tbody>
      </table>
</div>
<%- include('parts/bottom.ejs') %>
